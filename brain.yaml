brain: ranger
preset: default

# Instance settings
logLevel: info
domain: rizom.ai

plugins:
  directory-sync:
    git:
      repo: rizom-ai/rizom-ai-content
      authToken: ${GIT_SYNC_TOKEN}
  mcp:
    authToken: ${MCP_AUTH_TOKEN}
