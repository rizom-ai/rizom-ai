brain: ranger
preset: default
add:
  - atproto-registry

# Instance settings
logLevel: info
domain: rizom.ai

plugins:
  directory-sync:
    git:
      repo: rizom-ai/rizom-ai-content
      authToken: ${GIT_SYNC_TOKEN}
  atproto-registry: {}
  mcp:
    authToken: ${MCP_AUTH_TOKEN}
